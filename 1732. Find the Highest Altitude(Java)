class Solution {
    public int largestAltitude(int[] gain) {
        int max = 0;
        int maxFinal = 0;
        for (int i = 0; i < gain.length; i++){
            max += gain[i];
            maxFinal = Math.max(maxFinal, max);
        }
        return maxFinal;
    }
}
